---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  name: clusterimportcontrols.multicluster.crd.antrea.io
spec:
  group: multicluster.crd.antrea.io
  names:
    kind: ClusterImportControl
    listKind: ClusterImportControlList
    plural: clusterimportcontrols
    singular: clusterimportcontrol
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterImportControl controls which peer member clusters in the
          ClusterSet can connect or import resources into the appliedTo clusters.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            properties:
              allowedList:
                items:
                  properties:
                    clusters:
                      description: An empty ClusterSelector selects all member clusters
                        in the ClusterSet.
                      properties:
                        clusterIDs:
                          items:
                            type: string
                          type: array
                      type: object
                    resources:
                      items:
                        type: string
                      type: array
                  type: object
                type: array
              appliedToClusters:
                items:
                  type: string
                type: array
            required:
            - appliedToClusters
            type: object
        type: object
    served: true
    storage: true
