apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  creationTimestamp: null
  labels:
    app: antrea
  name: labelidentityimport-role
rules:
  - apiGroups:
      - multicluster.crd.antrea.io
    resources:
      - labelidentityimports
    verbs:
      - create
      - delete
      - get
      - list
      - patch
      - update
      - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  labels:
    app: antrea
  name: labelidentityimport-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: labelidentityimport-role
subjects:
  - kind: ServiceAccount
    name: controller
    namespace: changeme
  - kind: ServiceAccount
    name: member-access-sa
    namespace: changeme
